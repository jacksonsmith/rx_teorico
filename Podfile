# Podfile
use_frameworks!

target 'rx-teorico' do
pod 'RxSwift', '~> 4.2'
pod 'RxCocoa', '~> 4.2'
end

# RxTest and RxBlocking make the most sense in the context of unit/integration tests
target 'rx-teoricoTests' do
pod 'RxBlocking', '~> 4.2'
pod 'RxTest', '~> 4.2'
end
